import numpy as np
from PIL import Image as im
f=np.arange(800*300*3).reshape(800,300,3)
x,y=f.shape[0],f.shape[1]
for i in range(x): 
	for j in range(y):
		if(Vivaldi) 


f=Image.fromarray(f)
f.save("flag.jpg")
