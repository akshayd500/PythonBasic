import numpy as np
from PIL import Image as im

img = im.open("b.jpeg")
print (type(img))
img = np.array(img)
print (type(img))
x, y, ch = img.shape

"""for i in range(x):
	for j in  range(y):
		for k in  range(ch):
			if (i < x



//4) or (i > y*3//4):
				img[i,j,k] = 0
""



img[:500,:120,:]=50
img[380:,:500,:]=90
img[120:,0:500,:]=0
img[380:,0:500,:]=90
img = im.fromarray(img)
img.save("abc.jpeg")
